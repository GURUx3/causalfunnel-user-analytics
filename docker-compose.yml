services:
  mongodb:
    image: mongo:7
    container_name: user-analytics-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
